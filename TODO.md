# Backend: Express + Nodemailer Contact API

- [ ] Create `backend/` directory structure
- [ ] Add backend dependencies and scripts (`backend/package.json`)
- [ ] Implement Node/Express app with ES Modules (`backend/server.js`, `backend/src/app.js`)
- [ ] Add middleware: dotenv, cors, helmet, rate limiting, validation helpers
- [ ] Add `/api/health` and 404 handler
- [ ] Implement `POST /api/contact` route with zod validation + email sending
- [ ] Add Nodemailer Gmail SMTP transporter + professional HTML email template
- [ ] Add centralized error handling middleware with consistent JSON responses
- [ ] Add `.env.example` and document Gmail App Password setup
- [ ] Add Postman request example
- [ ] Integrate existing React `src/pages/Contact.tsx` to submit to backend API with loading/success/error toasts
- [ ] Run backend locally and verify endpoint with Postman

