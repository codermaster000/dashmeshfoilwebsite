# TODO

## Project Gallery — Premium Masonry Fix

- [ ] Update `src/components/site/ProjectGallery.tsx` to dynamically import **all** `src/images/*.png` via `import.meta.glob`.
- [ ] Remove any hardcoded 4-image behavior; ensure every discovered image renders.
- [ ] Replace the current grid-span/aspect-ratio inputs with a consistent cinematic masonry rhythm (large/medium/portrait/landscape mix).
- [ ] Remove/avoid overlap/collage behavior; use clean grid spacing and responsive stacking.
- [ ] Verify lightbox still works for each image.
- [ ] Run build / lint and visually confirm desktop/tablet/mobile.

